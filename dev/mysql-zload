#!/bin/bash

if [ ! -f $1 ]; then
	echo "$1 not a file"
	exit
fi

docker run \
	--rm \
	--read-only \
	-v $(realpath $1):/dump.sql.gz:ro \
	--network mysql \
	mysql:5.7 \
	/bin/bash -c "zcat /dump.sql.gz | mysql -h mysql -u root -proot ${2}"
